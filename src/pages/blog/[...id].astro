---
import ArticlePage from "@/features/content-site/pages/ArticlePage.astro";
import type { BlogEntry } from "@/features/content-site/lib/content";
import { getBlogPostPaths } from "@/features/content-site/lib/routes";

export const getStaticPaths = getBlogPostPaths;

const { post } = Astro.props as { post: BlogEntry };
---

<ArticlePage post={post} />
